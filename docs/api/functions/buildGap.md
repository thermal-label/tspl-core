[**@thermal-label/tspl-core**](../README.md)

***

[@thermal-label/tspl-core](../README.md) / buildGap

# Function: buildGap()

> **buildGap**(`pitchMm`, `offsetMm`): `Uint8Array`

`GAP <pitch> mm,<offset> mm\r\n` — pitch between gap-stock labels.

`pitchMm` is the gap (the thin transparent strip between labels —
typically 2–3 mm); `offsetMm` is usually `0`.

## Parameters

### pitchMm

`number`

### offsetMm

`number`

## Returns

`Uint8Array`
